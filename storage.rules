rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Uploads happen through the authenticated Next.js server and Admin SDK.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
